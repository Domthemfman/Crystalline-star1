import './globals.css'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Crystalline - Truth Seekers Network',
  description: 'An uncensorable safe haven for survivors to share experiences and seek truth',
  keywords: 'survivors, truth, blockchain, decentralized, community',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
